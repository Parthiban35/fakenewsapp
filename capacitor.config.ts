import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parthiban.fakenews',
  appName: 'Fake News Detector',
  webDir: 'build'
};

export default config;
