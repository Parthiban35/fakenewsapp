import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fakenewdetecter.com',
  appName: 'com.parthiban.app',
  webDir: 'capacitor.config.json.'
};

export default config;
